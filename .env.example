# Required: server-only GoHighLevel inbound webhook URL.
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/REPLACE_ME/webhook-trigger/REPLACE_ME

# Meta Pixel configuration. The connected Conversions API Gateway manages
# server-event forwarding; this application does not store a Meta API token.
META_PIXEL_ID=831171509159406
